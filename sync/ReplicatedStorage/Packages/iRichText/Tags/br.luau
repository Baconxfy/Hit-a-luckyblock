-- Line break tag (not used directly, handled in iRichText.luau)
return {
	Letterwise = false,
	Attributes = {},
	Apply = function(Label: TextLabel, _: { [string]: string }): { RBXScriptConnection }
		return {}
	end,
}
